I created a simple "game" using Javascript/JQuery. 

The user is prompted to input a number and the output is a customized grid of invisible div's which change color as the user moves over them. 
 
